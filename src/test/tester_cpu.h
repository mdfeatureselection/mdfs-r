// this is C header

#include <stdint.h>

double* run(
        int var_count,
        int obj_count,
        int dim,
        int dis,
        int div,
        uint32_t seed,
        float range,
        int with_decision,
        int stat_mode
);
